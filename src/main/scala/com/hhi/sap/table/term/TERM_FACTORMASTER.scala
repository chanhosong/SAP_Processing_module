package com.hhi.sap.table.term

class TERM_FACTORMASTER {
  val DATABASE = "sap".toUpperCase()
  val TABLE = s"$DATABASE.tmm_factor_master".toUpperCase()

  val COMPANYID = "companyid".toUpperCase()
  val SAUPBU = "saupbu".toUpperCase()
  val FACTOR_SEQ = "factor_seq".toUpperCase()
  val REGDATE = "regdate".toUpperCase()
  val REGTIME = "regtime".toUpperCase()
  val ZLAST = "zlast".toUpperCase()
  val FACTOR_NAME = "factor_name".toUpperCase()
  val FACTOR_RATE = "factor_rate".toUpperCase()
  val MATCH_FAIL_RATE = "match_fail_rate".toUpperCase()
  val MATCH01 = "match01".toUpperCase()
  val MATCH02 = "match02".toUpperCase()
  val MATCH03 = "match03".toUpperCase()
  val MATCH04 = "match04".toUpperCase()
  val MATCH05 = "match05".toUpperCase()
  val MATCH06 = "match06".toUpperCase()
  val MATCH07 = "match07".toUpperCase()
  val MATCH08 = "match08".toUpperCase()
  val MATCH09 = "match09".toUpperCase()
  val MATCH10 = "match10".toUpperCase()
  val UNAM = "unam".toUpperCase()
}
package com.hhi.sap.table.term

class TERM_MARA {
  val DATABASE = "sap".toUpperCase()
  val TABLENAME = "thd_mara".toUpperCase()
  val TABLE = s"$DATABASE.$TABLENAME".toUpperCase()

  val COMPANYID = "companyid".toUpperCase()
  val SAUPBU = "saupbu".toUpperCase()
  val MATNR = "matnr".toUpperCase()
  val MTART = "mtart".toUpperCase()
  val MATKL = "matkl".toUpperCase()
  val MEINS = "meins".toUpperCase()
  val BRGEW = "brgew".toUpperCase()
  val NTGEW = "ntgew".toUpperCase()
  val GEWEI = "gewei".toUpperCase()
  val XCHPF = "xchpf".toUpperCase()
  val ZZMAKTX1 = "zzmaktx1".toUpperCase()
  val ZZMGROUP = "zzmgroup".toUpperCase()
  val ZZDGIOX = "zzdgiox".toUpperCase()
  val ZZLEN = "zzlen".toUpperCase()
  val ERSDA = "ersda".toUpperCase()
  val LAEDA = "laeda".toUpperCase()
  val PGMID = "pgmid".toUpperCase()
  val CNAM = "cnam".toUpperCase()
  val CDAT = "cdat".toUpperCase()
  val CTIM = "ctim".toUpperCase()
}